# AGENTS.md — 项目约束（AI 助手与维护者必读）

## 工作流铁律：先本地打包，测试确认后再提交

1. **任何代码改动完成后，先在本地跑测试并打包出可安装的 DMG / 安装包**，把产物路径交给维护者实测。
2. **维护者实测确认没问题之前，禁止 `git commit`、`git push`、打 tag。**
3. 发版（`git tag vX.Y.Z`）必须在本地安装包验收通过之后进行；tag 一旦推送，CI 会自动创建公开 Release。

### 本地测试 + 打包命令速查（macOS）

```bash
# 1. 后端测试
cd desktop/backend
.venv/bin/python -m pytest tests/ -q

# 2. 前端构建（产物内嵌进后端二进制）
cd ../web && npm ci && npm run build

# 3. PyInstaller 打后端侧车
cd ..
python -m PyInstaller backend.spec --noconfirm --distpath backend-dist --workpath backend-build

# 4. Electron 出包（产物在 desktop/release/）
npm ci && npm run dist:mac
```

Windows 包无法在 macOS 上交叉构建（PyInstaller 限制），由 CI 在 Windows runner 上构建。

## 版本号规则（客户端 + 油猴脚本：先核线上版本，再 +1，只递增不回退）

**铁律：本地开发版本号 = 线上已发布的最新版本 +1 个补丁号（x.y.z → x.y.z+1）；禁止跳号（`1.2.4 → 1.3.0`）、禁止回退、禁止与线上版本持平。例外（跨小版本）必须由维护者明确指示并记录在案（如脚本 1.3.0）。** 开发和发布前必须实际核对线上版本，**以官方渠道页面显示为准，不要相信任何文档、README 或本文件里手写的版本号（可能过时）**：

- **桌面客户端**：打开 https://github.com/weige146/123Cloud/releases 看 Latest release 的 tag（`vX.Y.Z`），据此把 `desktop/package.json` 的 `version` 写成 X.Y.Z+1。发版 = 本地打包验收通过后 push tag `vX.Y.Z`，CI 自动以 tag 作为应用版本出包并创建公开 Release（打包时 `npm version` 用 tag 覆盖本地 version）。
- **油猴脚本**：打开 https://greasyfork.org/zh-CN/scripts/592236-123-%E5%8A%A9%E6%89%8B 看页面元信息的「版本：x.y.z」（页面正文/文档里手写的版本号可能过时，不要信），据此把脚本头部 `// @version` 写成 x.y.z+1。
- **客户端自动更新**（1.4.4 起内置，1.4.5 起 mac 闭环）：应用启动 10 秒后与每 6 小时检查 GitHub Release，发现新版后台自动下载，设置页「重启并安装」。mac 的 Release 必须同时上传 dmg 与 zip（electron-updater 的 MacUpdater 只认 zip，缺 zip 直接报错走「到发布页手动下载」分支），Windows 认 exe + latest.yml。1.4.4 及更早的已装版本提示「未包含自动更新组件/请到发布页下载」属预期——引导手动安装一次带更新组件的版本即可；mac 端 v1.4.4 的 Release 没有发 zip，同样要手动装一次 1.4.5 才闭环，之后版本互升全自动。

- 脚本文件：`油猴脚本/123-helper/123-helper.user.js`（各脚本一个同名文件夹，内含脚本本体、README 与 tests），版本号写在头部 `// @version`。
- 客户端当前线上版本：`1.4.7`（GitHub Latest Release，2026-09-18 tag 推送、CI 全平台出包：dmg/zip/exe）。**仓库即 `1.4.7` 已发布**。1.4.7 内容，1.4.7 内容：影库播放闭环——海报墙点播调用本地播放器（自动检测 IINA→mpv→VLC，可指定路径，支持任意 .app/exe 与系统默认）、播放记录（集内秒数断点续播 + 已看状态 + 继续观看栏 + 「看到第 N 集」一键标记）、Emby 式季海报墙详情与集列表选集播放、点播自动转存当季文件到网盘「秒传」目录、看完自动移入 123 回收站（见架构备忘 `library_playback.py`）；影库数据管理——取消按来源 JSON 删除的入口，删除挪到海报墙「管理」模式（多选删作品 + 整分类删），影库数据库可导出/导入备份（含 TMDB 整理结果与播放记录，重装一键恢复，见架构备忘）；影库分类调整——「频道」从电影/剧集两种升级为中文六频道（电影/电视剧/纪录片/综艺/动漫/儿童，按 TMDB 类型推断，旧库打开时自动回填），「状态」（更新中/已完结，TMDB 不准）筛选维度下线（见架构备忘）。1.4.6 内容：影库分类重写为 TMDB 海报墙 + 优爱腾式多维筛选（846d0a6）；TG 频道个例丢「网盘」按钮修复（长文案帖改走 Bot API 拆两条保按钮，见架构备忘）。1.4.5 内容：mac 自动更新闭环（dmg+zip 双产物、自定义换包安装绕开 Squirrel，见架构备忘）。
  - **TG 频道个例丢「网盘」按钮修复（2026-09-16，本地待实测，属 1.4.6）**：触发条件 = 频道发布渲染出的最终 HTML 文案 **>1024 字符**（简介 `render_overview_block` 不截断，TMDB 长简介帖必超）+ 有海报 + 配置了 TG Client API——命中后走 `send_telegram_photo_via_client`（Telethon 用户账号 `send_file`）合并发「海报+4096 长文案」单条消息，而**内联按钮是 bot 专属**：用户账号发的消息 Telegram 服务端会**静默丢弃按钮**（Telethon 源码确认 `build_reply_markup` 有构建进请求，白搭；第一版修复给 Telethon 传 buttons 实测无效即此因）。文案 ≤1024 走 Bot API `send_telegram_photo_then_edit_caption` 带按钮，所以只有长文案帖是个例；预览路径（`send_submission_preview_result`）长文案特意拆「海报+独立文字」走 Bot API，按钮永远在。**最终修法**：`send_telegram_rich_message` 只在 `reply_markup` 为空（无分享按钮，如超 256 字符的 fastlink 导出帖）时才走 Client API 合并长文案；带按钮的帖子一律走 Bot API——超长时拆「海报（无 caption）+ 文字（带按钮）」两条（与预览一致），按钮保证可见。**这是平台限制下的取舍**：用户账号发的单条长文案消息永远带不了按钮，合并单条与按钮不可兼得。连带：拆分分支的 `send_telegram_rich_message` 返回值新增 `photoMessageId`，`publish_once` 把它并入 `record_submission_publication` 的 seedMessageIds（海报无文字，历史清理按 TMDB 标记匹配不到，落库才能在旧帖清理时连带删除，防孤儿海报）。回归测试：`tests/test_submission_drafts.py::TelethonPublishButtonsTests`（带按钮长文案必须走 Bot API 拆两条且按钮在文字上/无按钮长文案走 Client API 合并/发布记录含 photoMessageId）。
- 脚本当前线上版本：`1.3.15`（核对于 2026-09-20，greasyfork 更新直链 @version；1.3.15 已发布：动漫拆季真实分季数据，即下文 1.3.15 内容）。**仓库已升 `1.3.16`**（本地待实测），1.3.16 内容：整理识别五连修（即下文「1.3.15 二批」，版本随发布顺延为 1.3.16）。1.3.15 内容：动漫拆季策略接真实分季数据——「单季顺序拆多季 / 单季跳序拆多季」的季集表从单一 TMDB 升级为三源可选（CureTMDb 社区分季表 > Bangumi 期结构 > TMDB 官方分季兜底），顺序拆从「顺序占位数格子」重写为「对号入座」（集号当累计绝对集数在真实结构里反查季集，缺集不错位、合法季集保留、TMDB 合并单季的多 cour 番也能拆），「多季合并一季」同链接入真实结构（文件 SxxEyy 按期链折算成全作累计集数并进单季：狐妖小红娘 S13E05 → S01E172，只整理其中一季也能算对；无真实数据退回顺序累加），单季合并型作品（tv.json 死神 366 集）不硬拆并提示改用合并策略（见架构备忘「动漫拆季真实分季数据」）。1.3.16 内容（2026-09-20，本地待实测）整理识别五连修：①【频道/字幕组/标记】前缀不再抢占标题——`inferTitle` 重写为「括号内候选 vs 括号外残余」双候选取舍：`bracketContentLooksLikeTag`（频道/电视台/卫视/字幕组/工作室等后缀词表 + 清晰度/语言/集数注记词表 + 季集标记剥掉后剩不足 2 汉字判整括号为注记）过滤标记括号；括号内候选与括号外标题都有且不同时，括号内是 ≤6 字短词且括号外无任何标记（`stemHasTagMarker`，含「星海飞驰篇」式副标题后缀）才取括号外——`【重温经典频道】黑猫警长`→黑猫警长、`[字幕组]番名`→番名、`【琅琊榜】DVD全集`→琅琊榜、`【凡人修仙传】星海飞驰篇`→凡人修仙传；顺带 INFER_TITLE_TAIL_RE 支持剥「76集全」、标题清理认识全角（）。② TMDB 搜索综合兜底——手动查询（organize-search-run）同时搜所选类型 + multi 合并去重并按「精确同名>包含>年份>所选类型>热度」排序、ID 查询类型 404 自动换另一类型；自动识别（resolveMedia/validateStrongFolderTitle）单一类型无果时 `searchTmdbCandidates` 以 `typeOverride:""` 重搜一轮；空结果提示换词/填 ID（candidateRail 空态）。③ 18+ 影视——TmdbClient.search 支持 `includeAdult`，常规轮**全空**才带 include_adult=true 补一轮（自动 + 手动同规则），正常资源请求量不变。④ BluRay.1080p.Remux 补升——分辨率段隔断时 Remux 别名只命中单独 Remux 档，按名字 BluRay/UHD/BD 有无补升（油猴 inferTechnicalFields + 桌面端 movie_library.update_detail_state 同步，纯 Remux 不误升）。⑤ 来源@发布组拆分——release-group `result()` 出口 `splitSourceGroupTag`：@ 前缀命中 mediaSource 别名表（动态取 DEFAULT_FIXED_MAPPINGS）时发布组取 @ 后段（NF@ADWeb→NF+ADWeb），`cXcY@FRDS` 类小组@大组不动。连带：inferMediaType 认识 全N集/共N集（年份+分辨率不再误判电影）。回归：tests/title-brackets.test.mjs（19 项）+ technical-fields 6b + test_movie_library.py::test_infer_technical_remux_resolution_gap。1.3.12 内容：秒传导入/导出频控根治——自适应限速门（列举/写入各一把全局门，撞限全局冷却、AIMD 提速、水位入断点）、每页 500 + 建目录名索引、接口线路 auto 探测与运行中自动换道、秒传未命中单列一桶不再算失败、进度条运行明细（目录/文件/体积/速率/ETA/冷却倒计时 + 最近 30 行日志）与暂停/继续、结果页按状态筛选与 CSV 导出、更新内容通知（见架构备忘）。1.3.11 内容：整理识别修复——帧率误读（`H.265.25fps` 被读成 `265.25fps`）与 REMUX 丢识别、登录会话改客户端中转 + 按来源网盘页跳转、删除剪贴板导出/粘贴导入与会话设置 UI（见下）。1.3.9 内容：登录会话导出/导入（跨浏览器复用不新增设备）、秒传接口线路直连 api.123278.com（fastLane 开关）、秒传导入「自动清理+无效跳过+千万级流式」、**user. 登录域名识别**（`isOfficialPanPortalHost` 白名单放行 `user.` 子域——user.123pan.cn 是登录/账号域名，旧正则只认 www|yun 导致脚本在登录页不启动，「导入登录会话」必须先登录才能用，登录又新增设备，复用失去意义；且 user. 域名不服务 /b/api，脚本 API 主机在该域名下回落 `https://www.123pan.cn`；回归测试 `tests/portal-host.test.mjs`）。1.3.8 时期内容：重命名历史条目显示可辨认化——条目名落库时改用 `renameHistoryLabel`（单目录=目录名、多目录=N 个目录、查不到退回"批量重命名"），下拉选项改为「名字 · N 项 · YYYY-MM-DD HH:mm」，此前全是"批量重命名 · 日期"没法分辨哪次操作；重命名落在网盘根目录时父目录显示「根目录」而不是「目录 0」（`renameParentDirName` 特判 id 0：根目录不是可查询的目录条目，fileInfos 查不到名，分组兜底曾把它渲染成「目录 0」；操作记录分组与历史条目名共用此函数）；恢复列表按当前文件夹过滤（`filterRenameHistoryForDir`：历史本身全局存储，但只显示 targets 全部落在当前目录的条目，别的文件夹的重命名不再串场出现）；恢复改为两级交互（维护者 2026-09-13 定稿）：弹窗底部**只有历史入口、无恢复按钮**——用**自制下拉菜单**（`.history-menu`/`.history-popup`/`.history-item`，纯 click 事件委托）列出当前目录的记录，**不要用原生 `<select>`**：change 委托在这个弹窗里不可靠（单条记录重复选中不触发 change、弹层展开期间 DOM 被替换会让 change 派发到已脱离节点，踩过两次坑）。点任意一条记录 → 翻开该记录明细页（`renderRenameRestore`：改动点列表「改名前 → 改名后」，页面即恢复预览），页内「确认恢复 N 项」（`undoRename` 取 `rename.restoreId`）才真正执行，「返回」清 `restoreId` 回规则视图；点弹层外任意位置收起列表（handleClick 统一处理）。执行重命名后不自动跳明细页（走原结果页）。历史 1.3.7 内容：分享直投（submitShares 改发结构化 links 跳过归属判断，旧客户端自动回退拼接文本）、误报修复（草稿数达标即成功，sentCount 不再一票否决——Bot 未配置/推送失败时草稿已入库；超时/断连文案改"结果未知"防误导）、整理预览镜像层 word-break 统一 break-all 修复季集红字漂移。1.3.6 = 整理预览季集编号高亮由荧光黄底色改为红色字体。**`1.3.0` 已随仓库推送**（维护者 2026-09-08 明确指示跨过「只 +1 补丁号」规则直接升 1.3.0，作为例外记录在案），1.3.0 内容：fileInfos 33 条截断补查、分享链接生成秒传的风控实测修复（旧域名直连 123865 + 分车道限速门 + 32 目录并发）、秒传导出/导入并发按接口实测对齐（8/32）+ 100011 频控退避重试、TMDB 校准与媒体整理增强、选中项出口统一按文件名自然排序。
  - **动漫拆季真实分季数据（2026-09-18，本地待实测，属 1.3.15）**：参照 MoviePilot 插件 wikrin/MoviePilot-Plugins `curetmdbanime`（Go 本地反代改写 TMDB 响应 + Bangumi/tv.json 数据源 + 四层评分决策引擎）改造脚本拆分策略，只取其数据源思路与 absolute_episode 对号入座核心，**不移植评分引擎**（脚本整理是交互式预览 + 手动锁定，人在回路；对号入座已是确定性映射）。
    - **数据链**（`src/tmdb.js` 区段 Bangumi 客户端 + organize-strategy 区段纯函数）：结构对象 `{source, sourceLabel, name, detail, seasons:[{season,count,name,date}], incomplete}`；优先级 curetmdb > bangumi > tmdb（`tmdbStructureFromMedia` 永远兜底，`resolveStrategyStructure` 冷缓存回退）。CureTMDb tv.json = `raw.githubusercontent.com/wikrin/CureTMDb/main/tv.json`（按 TMDB id 索引，**目前仅 7 部**：100个女朋友/药屋/胆大党/更衣人偶/青春猪头/咒术回战/死神，死神=S0 4 集特别篇+S1 366 集本篇的合并型），GM 缓存 24h（`Cloud123.Helper.CureTmdbTvJson`；raw.githubusercontent.com 大陆被墙，拉不到按源失败处理不挡 Bangumi）。Bangumi 端点实测（走反代核过）：搜索 `POST /v0/search/subjects`（**复数**，单数 404）、条目 `GET /v0/subjects/{id}`（`platform`、`eps`=正片集数、`total_episodes` 含特典，容量取 eps>0?eps:total_episodes）、关联 `GET /v0/subjects/{id}/subjects`（**不是 /relations**；relation 取「续集」；关联项不带 platform/eps，候选必须再拉详情确认）。**官方 api.bgm.tv 大陆被墙**：默认走公共反代 `https://bgmapi.anibt.net`（维护者提供；响应里图片域自动改写 bgmimg.anibt.net），失败自动回退官方域名（`bangumiActiveHost` 会话内记忆）；UA 按官方礼仪带脚本标识。**平台过滤必须 TV/WEB 都认**（`bangumiPlatformIsSeries`）——国产动画/网盘番在 Bangumi 全是 `platform:"WEB"`（实测凡人修仙传），只认 TV 整个国产区失配；剧场版 platform 是「剧场版」天然排除。**匹配是两级**（维护者实测首版精确相等匹配不到 → 「找不到 Bangumi 卡片 + 单季护栏拒拆」双重现象）：归一化标题相等 > 一方包含另一方（凡人修仙传 vs 凡人修仙传之凡人风起天南），排序 精确>包含>年份近>标题更短（裸标题通常是第一期）>日期早>译名命中；搜索按 中文标题→原名→其他中文别名 最多三个关键词重试（`mediaBangumiSearchKeywords`），全部网络失败抛错（弹窗可重试）、确实搜不到返回 null（弹窗显示「未匹配」说明行）。
    - **Bangumi 串链**（`buildBangumiChainSubjects`）：搜索结果按 `matchBangumiSubject`（type=2 + platform=TV + 归一化标题相等（复用 `episodeGroupNameKey`）+ 首播年与 TMDB 年份 ±1；并列时译名命中/年份近/日期早优先）锚定第一期；沿「续集」正向串，**不做前传回溯**（龙珠Z 的前传是龙珠，同 IP 不同作会串台）；词干相容（一方包含另一方）且**词干固定取锚点标题**——用逐跳当前期判断会断链（长 cour 名「怀玉·玉折/涩谷事变」与「死灭回游」互不包含）；日期不回退、链长 ≤40、每期必须 platform=TV；集数未知的连载期照收（count=0 → `incomplete`），拆分只覆盖已知集数。结构缓存 `Cloud123.Helper.BangumiStructure:{tmdbId}` 24h。
    - **对号入座算法**（`planSplitSequentialAssignments` 重写）：`splitSeasonBounds` 把结构展开成累计区间；文件自身 SxxEyy 在结构里合法 → 原样保留（不再被重编号）；否则集号当累计绝对集数反查区间（季内重编号 `E - from + 1`）；无集号文件排在已入座最大位置之后顺序占位（无编号批次不受影响）；重复集号第一条占位、后续顺延 + 警示；跨季 span 整条挪下一季从 1 重编号；超总集数顺延最后一季（旧语义）。**金标兼容**：完整有序 1..N 批次 + TMDB 结构输出与旧顺序占位算法逐文件一致（`organize-strategy.test.mjs` 龙珠Z 50 集用例锁定）。
    - **护栏**：单季结构不硬拆——警示「改用合并策略」；判定按**结构期数**（`splitStructureIsTriviallySingle`：seasons 数组长度），连载中第二期集数未知（bounds 只剩一季）**不算**单季。跳序拆吃 bangumi/curetmdb 结构时警示「该分季数据按期重新编号，建议改用顺序拆」（跳序语义=绝对连续编号，只适配海贼王式 TMDB 结构）。
    - **UI**（复刻剧集组模式）：**打开策略弹窗即预取分季数据**（24h 缓存，选拆分卡片立即可见）；`loadOrganizeSplitStructures` 并行拉两源（`Promise.allSettled`），数据源卡片列季集 chips + 匹配条目名 + loading/error/重试 + 「未匹配」说明行（`structuresInfo`），TMDB 永远在列表兜底；应用时 `{kind, structureSource}` 写进 `strategies[groupId]`、结构写进 `organize.structureCache["tv:{tmdbId}:{source}"]`；**选中单季结构点确定会被拦截**并 toast「只有一季，无需拆分」（此前静默应用出空结果，维护者实测困惑点）；第一次加载失败后再点拆分卡片会重试（不能拿 structuresError 挡自动加载）；`organizeStrategyLabel` 带来源后缀（「单季顺序拆多季（Bangumi）」）。外部源全失败保留 TMDB + 错误提示，绝不静默瞎拆。回归测试：`tests/organize-strategy.test.mjs`（对号入座/单季护栏/匹配含 WEB+包含/串链/结构工具）+ `tests/anime-split-realdata.test.mjs`（客户端反代回退/UA/tv.json 缓存/端到端串链/失败分类/WEB 平台 incomplete）。真实反代端到端实测（2026-09-18）：咒术回战 S1:24/S2:23/S3:12(连载)/S4:0、间谍过家家 4 期、凡人修仙传（WEB）2 期、死神社区表单季 366。
    - **踩坑**：① 测试切片 `// src/api.js` 标记在第 139 行——`utils.js → api.js` 只有小工具段；tmdb.js 要单独切 `// src/tmdb.js → // src/metadata.js`，且 organize-strategy 切片还依赖 categories 段的特殊篇关键词表（`DEFAULT_SPECIAL_KEYWORD_MAPPINGS` 是加载期顶层调用），四段组合见 `anime-split-realdata.test.mjs`；② BangumiClient 内存缓存连空搜索结果也缓存，E2E 测试阶段之间要 `cache.clear()`；③ vm 沙箱产物与主沙箱原型不同，沙箱数组 `.map()` 的返回值仍带沙箱原型，deepEqual 前必须过 `plain()`。
    - **合并策略接真实结构（2026-09-18 二轮，维护者实测狐妖小红娘触发）**：`planMergeAssignments` 吃到 bangumi/curetmdb 结构时走 `planMergeStructureAssignments`——文件 SxxEyy 在期链里定位出全作累计集数（`from(季) + E - 1`），全部并进单一目标季（默认第 1 季）：狐妖小红娘 13 篇累计前 12 篇 167 集，S13E05 → S01E172，**只整理其中一季也能算对**（偏移来自结构不依赖其他季的文件在场）。单期结构（柯南式全作一个 entry）集号即绝对集数；季号不在结构里/超出对应期集数 → 顺序排到已入座位置之后 + 警示；重复集号顺延 + 警示。tmdb 源（含冷缓存回退）保持旧行为（最小季号起顺序累加）。跨同名分组合并 `prepareMergeStrategyAssignments` 增加 structureCache 参数贯通（organizeBuildOptions 传入），否则跨组合并拿不到结构。UI：merge 也走数据源卡片（`strategyUsesStructure` = 两种拆分 + merge）、标签带来源后缀、默认选中真实源（merge 连单期结构也优先——柯南式照样有用）；单季拦截 toast 只限拆分策略（合并吃单期结构合法）。真实反代验证：狐妖小红娘串出 13 期（下沙 13/王权 14/月红 21/千颜 15/南国 26/竹业 12/尾生 10/金晨曦 10/沐天城 12/两生花 12/无暮 12/镜花缘 10/黄风岭 16），S13E01/E05/E16 → S01E168/172/183。回归：`organize-strategy.test.mjs` 合并真实结构三用例（多期折算/单季在场/单期结构+季号越界）。
    - **踩坑（三轮：剧集组策略防错位守卫）**：⑤ `matchEpisodeGroupFiles` 的「绝对集号」兜底会把文件集号当剧集组顺序号——狐妖式文件 S13E05 在单季剧的剧集组里 origin 13x5 对不上后，掉进兜底错配到全剧第 5 集（正确 ≈ 第 172 集）。守卫：文件**显式季号**（`parseSeasonEpisode(name, 0)` 取，0=无季号标记）不在剧集组 origin 季集合里时跳过该兜底，只信季集精确/播出日期/集名，对不上保留原识别 + 未匹配警示；无季号的绝对集号文件不受影响。⑥ 策略弹窗单季卡片文案按 pick 区分：merge 下显示「单期结构：集号即全作累计集数」，拆分下才显示「请改用合并策略」（此前 merge 场景文案自相矛盾）。
    - **踩坑（二轮）**：④ 「狐妖小红娘1 下沙篇」式命名——每期名自带编号（小红娘1/2/3…），锚点与续集名互不包含，原「双向包含」词干相容直接把链掐断在第一期（表现=结构只剩 1 期、合并全错）。修法 `bangumiSequelNameCompatible`：从锚点 key 剥「系列词干+期号」的词干段（首个 `第N季/篇` 或数字位置之前），候选名 = 同词干 + 新期号开头也算相容；龙珠GT 的 gt 非期号仍拒。**教训：手动逐跳验证绕过了脚本的相容判断函数，暴露不了这类 bug——必须用完整链路函数（loadBangumiStructureForMedia 端到端）做真实验证。**
  - **分享直投丢提取码回归（2026-09-13 修复）**：客户端 v1.4.2（commit 0ba3e60）给 `/api/submission/submit` 加的结构化 links 直投分支里，link_dicts 的 `cleanUrl` 写成了 `url.split("?")[0]` 把 `?pwd=` 剥掉了；而草稿正文 `{shareUrl}`（`render_share_url_value`）与 TG 消息"网盘"按钮（`build_submission_preview_markup`）渲染的都是 cleanUrl，于是推送出去的草稿链接不带提取码（password 其实存进了 `share.password`，只是没人渲染）。老文本路径的 cleanUrl 走 `normalize_web_share_url` 一直保留查询串所以没事。修复 = 直投分支 cleanUrl 同样走 `normalize_web_share_url`（保留 `?pwd=`）。**脚本侧不改**（维护者 2026-09-13 明确要求：提取码本来就在 url 的 `?pwd=` 里，脚本没毛病，不往 payload 里加 password 字段）。回归测试：`tests/test_submission_routing.py::test_direct_links_keep_pwd_in_clean_url`、油猴 `operation-records.test.mjs` 的 renameHistoryLabel 用例。**铁律：任何路径构建 123 分享的 cleanUrl 都不许剥查询串，提取码就在 `?pwd=` 里。**
  - **秒传线路直连 api.123278.com（2026-09-14，已随 1.3.9 发布）**：维护者实测 `https://api.123278.com/b/api/file/upload_request` **16~24 线程 ≈150 次/秒**（默认域名 123865 为 32 并发 85 次/秒），1400 万级导入纯转存可从 ~46h 压到 ~26h。实现（秒传设置新增「fastLane」开关，默认关）：`Pan123Api.laneHost` + `runFastlinkTask` 在 import/cloudImport/export 任务窗口内置 `FASTLANE_HOST`，`reuseFile`/`createFolder`/`listPage` 带 `host` 直连；request() 内置两段式回退——镜像网络错误（TypeError）/超时（无 signal 的 AbortError）/403/404/501 都当场 `markLaneDead()`（会话内摘除 + 清 laneHost）并**同一轮**改打默认域名，不烧重试次数；镜像请求强制 `credentials:"omit"`（鉴权走 Bearer 头，跨域带 cookie 会被 CORS 拒收；**注意不能无脑覆盖所有 host 覆盖的 credentialsMode**，分享线路同源请求要 include——踩过一次 share-pacing 回归）；用户主动取消（signal 已中止的 AbortError）原样上抛不回退。并发：镜像线路下「写入并发」设置 ≥16 生效、否则默认 24（下限 8 上限 64）；默认线路保持 32 底不变。**维护者提供但未接入的接口**：OpenAPI 列目录/移动/设备列表/真实上传链（s3_upload_object 等）——需要 OAuth token，网页脚本会话里没有，桌面端影库转存（library_transfer.py 的 md5_reuse+ensure_path）已在用 OpenAPI；真实上传链对秒传导入无用（未命中不上传原件）。回归测试：`tests/fastlink-fastlane.test.mjs`（直连/关闭/回退/404/用户取消/超时/并发策略）。
  - **登录会话导出/导入（跨浏览器复用，不新增设备）（2026-09-14，已随 1.3.9 发布）**：脚本此前每个浏览器登录都在 123 云盘新增一台设备。新增「登录会话复用」：`buildSessionExportPayload` 导出当前浏览器 localStorage 的原始 `authorToken`+`LoginUuid`（LoginUuid 可能是站点加密形态，**必须原样导出原样写回**，与官方 SPA 完全兼容；不要解密后写回），`applySessionImportPayload` 在目标浏览器写回并刷新页面，SPA 与脚本共用同一会话。入口两处：设置 → 常规 →「登录会话复用」按钮（`session-export`/`session-import`，导入走 openInputDialog，写完 800ms 后 reload）；油猴菜单「导出/导入登录会话」（`registerSessionMenu`，不依赖面板 UI，登录页/未登录状态也能导入——这是新浏览器免登录导入的主路径）。解析容错：JSON（c123Session 标记）或 `token|uuid`/两行分隔；导入失败不破坏现有会话；token 过期后需重新导出导入。注意：官方桌面客户端的 token 未必兼容网页 /b/api 会话（未实测），导出导入链路按网页会话设计。回归测试：`tests/session-reuse.test.mjs`（往返一致/容错解析/非法输入不破坏/未登录报错）。
  - **秒传导入「自动清理 + 无效跳过 + 千万级流式」（2026-09-14，已随 1.3.9 发布）**：起因是维护者实测 115 导出 JSON《数字涵芳阁》（2.45GB / **1400 万条**，实测 ~112.7 万条路径含连续空格、~2 万条含 `#$%?*<>:|`、etag 99.8% 为 21-22 字符 base62）在旧逻辑第 2 条就整包报错「路径包含不支持的目录或字符」。改动（全部只在导入路径生效，拆分/转换/导出保持严格模式零行为变化）：① 导入语义路径校验 `resolveImportedPathParts(_, _, sanitize)`——路径段先过 `cleanPathPart` 清理（连续空格折叠单空格、`\/:*?"<>|` 替换、`#$%` 删除、去首尾点空格）后照常导入并计入 `sanitized`；**点开头段（.stfolder/.aria2 等隐藏目录/文件，维护者明确要求跳过）整条跳过**、清理后仍为空/`.`/`..`、坏 Etag、坏大小 → 分类跳过计数（reasons: path/hidden/etag/size），不再整包中止，样本封顶 50 条；② 本地大 JSON（>8MB）走**流式批次导入**：scanner 批次模式（`batchSize`+`takeBatch`+`getCommonPath`，批次 8192）每攒满一批交 `createFastlinkStreamImporter.consume()` 消费完再继续读文件（文件流天然背压），条目不再全量驻留内存，目录放弃全量预建改逐条 `ensurePath`（promise 缓存去重并发）；③ **断点 v3**（`Cloud123.Helper.FastlinkImportStreamProgress` + `#stream-shard*`，按根目录匹配、不再依赖内容指纹）：done 集合存 importKey 的 64 位滚动指纹（`fastlinkKeyFingerprint`，hex16 分片落盘），内存 `createFastlinkDoneSet` = BigInt64Array 有序主段 + 小段有序尾巴二分（1400 万条约 112MB，取代 v2 全量 key 字符串的 2-3GB）；全成功清档、异根目录隔离、遇旧档清掉重开；④ 结果带 `invalid/sanitized/invalidReasons/invalidSamples`，结果页新增「已导入跳过」统计与黄条说明（失败样本封顶 200 条）。**实测预期**：1400 万条按 85 req/s 纯转存 ≈46 小时 + 目录查询（list ~15 QPS），要连续跑数天靠断点续传，建议先用拆分功能切小样验证 etag 秒传命中率再全量跑。回归测试：`tests/fastlink-skip-invalid.test.mjs`（严格模式不变/清理+跳过计数/流式≡文本/批次交付/commonPath 头部可读/doneSet/断点续传/端到端大文件/文本路径透传）。
  - **秒传频控根治：自适应限速门 + 线路探测 + 运行明细（2026-09-15，本地待实测，属 1.3.12；方案文档 `油猴脚本/123-helper/PLAN-fastlink-pacing.md`）**：起因是维护者对比第三方 `123FastLink` 脚本——它能正常导出/导入不报"请求过于频繁"，我们还看不到扫描明细。**根因不是接口不同，而是三件事**：① 把并发当速度手段（导出扫描 8 目录并发 `listAll`、翻页零间距；导入 reuse 32 并发 + `ensurePath` 掺在同一 `file/list/new` 的 ~15 QPS 配额里），② 撞限后每个 worker 各自 6 次指数退避、退避完又同时打回同一个风控窗口（6 次烧完整轮判死），③ `findChildFolder` 判断子目录是否存在要用 `listAll` **全量翻页**列父目录（几千条目的目录一个子目录 30-50 个请求），外加默认 host 打的是**页面域名**（`*.123pan.cn`，前面 CDN/WAF 按 IP+窗口计数，实测不限流的是 `www.123865.com`）。参考脚本能稳是因为：串行 + 每页 500 + 主动 50ms 间距 + 连续 3 次限流**全局**暂停 8s（AIMD 的乘性减），不是因为它用了别的接口。
     - **门**：`createApiGate(profile)`（`src/api.js`）——全局最小间距（`waitTurn` **预约时隙**：`nextFree` 管同波内排队、`lastStart + interval` 管顺序请求间距。**注意**：旧写法只比较 `lastStart`，同一瞬间进来的 N 个请求都算出 `gap<0` 一起放行，等于只约束波与波之间、不约束同波内并发——这是第一次跑 `gate-pacing` 才暴露的缺陷）；`hit()` 冷却 + 间距放大（`decreaseFactor`，0 = 一步踩到 `maxInterval` 的分享接口旧语义）、`ok()` 需连续成功 `okStreakTarget` 且距上次撞限超过 `recoverAfterMs` 才减半提速、`snapshot()/restore()` 随断点往返、`configureFor(host)` 按域名切车道。`shareApiGate` 改为该工厂实例（保留 `fastLane/slowLane` 读写访问器，旧测试整体替换车道对象的写法照旧可用）。`panApiGates.list`（`file/list/new`、`file/info`、`file/detail`）与 `panApiGates.write`（`upload_request`）由 `PACING_BY_PATH` + `gateForPath(path)` 在 `request()` 内**自动挂**，调用方传 `pacing: null` 关闭；分享接口不写进自动表（它有自己的 host 候选与冷却重试，否则会双重排队）。
     - **请求层预算**：撞频控（`isRateLimitedError`：`100011`/429/频繁类文案）走"等门开"，不烧 `attempts`（`paceRetryCap` 默认 3 次 + `paceWaitBudgetMs` 默认 20s 双闸），超预算给错误打 `paceExhausted` 直接终局上抛（外层 catch 见标记不再重投）——单条失败由上层记成失败继续跑，绝不为一条把整轮冻住。**签名与超时计时必须紧贴 fetch**：`signedQuery` 和 `setTimeout(timeoutMs)` 都挪到 `waitTurn` 之后、每线路尝试各一次（否则跨分钟旧签名被拒、一次 45s 冷却被 30s 请求超时误判成故障），每线路尝试用独立 `AbortController`。挂门时不再叠加指数退避；`TimeoutError` 在挂门且 `attempts>1` 时按重试预算重试（旧行为是默认域名一次超时整轮判死）。
     - **削减请求**：`listLimit` 默认 500（夹 100~1000，探测可下调）、`listAll` 加 `maxPages` 与"连续两页 FileID 相同即止损"、`findChildFolder` 改查 `childFolderIndex`（`名字→ID`，索引以字符串键挂在任务的 `listAll` promise 上、随任务回收）、`rememberCreatedFolder` 新建覆盖表（api 实例级，超 4000 个父目录整表丢弃）、`createFolder` 缺 ID 兜底先剔除过期 listing 再查索引。扫描并发语义改成"流水深度"（默认 24、上限 64），速率归门管。
     - **线路**：`DRIVE_ROUTE_KINDS` = page/canonical(`www.123865.com`)/mirror(`api.123278.com`)；`probeDriveRoute` 各拉两页（`pacing:null` 拿裸延迟与真错误）按「可用 → 不撞限 → 更快」排序（同分按 mirror>canonical>page 固定偏置，避免抖动），顺带确认大分页是否被吃下；结果按 origin 存 GM 缓存 TTL 30 分钟；`beginDriveRoute` 默认走 auto（**设置页不提供线路上拉与测速按钮**——维护者 2026-09-16 要求删掉，只留内部状态 `fastlinkTools.routePreference: auto|mirror`，`mirror` 仅作为旧 `fastLane` 开关的兼容位：只走镜像域名且不自动换道）；`noteRouteThrottle` 在同一线路 60s 窗口内连撞 6 次 → 换下一候选并清零门水位。线路选错时的唯一出口是「秒传设置 → 恢复默认」（清 routePreference + 探测缓存，下一次任务重新探测）。
     - **导入语义**：`reuseFile` 未命中抛 `error.fastlinkMiss = true`，流式与数组路径都单列 `miss` 桶（不算 `fail`、不写 done 指纹）；`finish()` **只在无失败且无未命中时才清断点**（未命中条目没写 done，清档会让下次重跑把已成功条目再转一遍、`duplicate:1` 下生成"名字 (1)"副本）；数组路径收尾保留断点的判据同样从"有失败"改成"有非 success"；`batchResult` 新增 `miss`。断点 v3 主记录新增 `pacing:{write,list}` 与 `bytesDone`。
     - **运行控制与明细**：`panRunControl`（暂停/继续）挂在 `request()` 取号前，只对挂了门的链路生效，导出扫描/目录预建/秒传写入共用一个开关（在途请求跑完即停在门口，断点照常落盘）；`createRunLedger`（内存环形 200 行 / 展开 800 行）吃 `onDetail` 与 `apiPaceListener`，产出目录数/文件数/体积/速率/ETA/当前条目/频控倒计时的一行摘要塞进进度条，展开面板渲染最近 30 行（`aria-live="off"`）；结果页新增按状态筛选（`filterResultDetails`）与 `buildResultCsv` 导出。**千万级场景明细只进环形缓冲，绝不整表驻留或往 DOM 灌**。
     - **更新内容通知**：`SCRIPT_CHANGELOG` 头部版本必须与 `@version` 一致（`changelog-notice.test.mjs` 守）、`maybeShowUpdateNotes` 按 `Cloud123.Helper.SeenChangelog` 每版本弹一次且只弹最新一条（`limit:1`）；**无常驻菜单入口**——1.3.13 撤掉「查看 123 助手更新内容」菜单（维护者 2026-09-17 指示：安装/升级后自动弹一次最新版说明即可，别处不放入口）。**日志文案尽量小白化（维护者 2026-09-17 指示）**：写给普通用户看——讲清楚这次更新用户能得到什么，用「撞名/重名」「回收站」「还原」这类大白话；不写「兜底、盲区、预检、阻塞、实时复核、任务内」这类内部术语和 AI 腔；一条只讲一件事，不堆括号补充。
     - **踩坑记录**：① 门的水位不能只靠 `attempts` 兜底——第一次实现把 60 次"等门开"当成预算，遇到一直返回 100011 的用例直接挂住 45 分钟级等待，改成「次数 + 累计时长」双闸并打 `paceExhausted`；② 回归测试按标记切片，新增跨模块引用会把依赖带进沙箱（`fastlink-secondary` 因 `collectFastlinkFiles` 引用 `panApiGates` 而必须补 `// src/api.js` 切片），且 vm 沙箱不一定有 `Symbol`，挂在 promise 上的键用字符串；③ 结果页筛选 `<select>` 走 `handleChange` 的 id 分支，`data-action` 只对 click 有效；④ 测试里整体替换 `gate.lanes` 时 `configureFor` 会覆盖实例字段，改门参数要连 `lanes` 一起改。回归测试：`gate-pacing`（机制）、`request-gate`（请求层行为/暂停/取消/每页 500 与止损）、`drive-route`（探测/固定档/auto 换道/旧配置映射）、`fastlink-import-miss`（未命中分桶与断点）、`run-ledger`（明细计数/封顶/转义/筛选/CSV）、`changelog-notice`（版本一致性与门控）。
  - **整理识别修复：帧率误读 + REMUX 丢识别（2026-09-14，本地待实测，属 1.3.10）**：两处都在 `inferTechnicalFields`。① 帧率：`H.265.25fps` 的点分字段会被 `(\d{2,3}(?:\.\d{1,3})?)fps` 整段吞成「265.25fps」（左最匹配先把编码版本号并进数字）；修复 = 匹配值带小数且 >120（真实小数帧率最大 23.976/29.97/59.94/119.88）时只保留 fps 紧前一段 → 「25fps」，`23.976fps` 等真实小数不受影响。② 资源类型：`UHD.BluRay.2160p.REMUX` 里 Remux 别名（`UHD[ ._-]*BluRay[ ._-]*Remux`）跨不过中间的分辨率段，会先被 `UHD BluRay` 条目命中（映射表按数组顺序首个命中即返回），REMUX 整个丢掉、新文件名不含 Remux；修复 = 名字带 `\bREMUX\b` 记号且已命中 `UHD BluRay`/`BluRay` 时补升为 `UHD BluRay Remux`/`BluRay Remux`（紧邻的 `UHD.BluRay.REMUX` 本来就能命中，不受影响）。回归测试：`tests/technical-fields.test.mjs` 用例 5/6。
  - **登录会话导入跨域接力（2026-09-14，本地待实测，属 1.3.10）**：localStorage 按域名隔离，网盘登录态（authorToken/LoginUuid）只在网盘页域名生效——维护者实测在登录页 `user.123pan.cn` 导入后刷新没反应（写入的是登录页域名的 localStorage，登录页状态机不认），手机浏览器也栽在这里。最终方案（`completeSessionImport` 两分支）：在任一网盘页域名（`www/yun.*` 各官方域名，`isPanSessionOrigin` 判定）拉取 → 直接写 localStorage + 刷新（原行为）；在登录页 `user.*`/其他页面拉取 → **只暂存并自动跳转到「推送来源网盘页」**——推送时把 `location.origin` 一并 POST 给客户端存档（后端 `_normalize_pan_session_origin` 只留 scheme://host），拉取响应带回 origin，缺省 www.123pan.cn，非网盘域名不采纳。来源页 `start()` 的 `consumePendingSessionHandoff()` 发现暂存（`GM_setValue` 跨域名共享，`SESSION_HANDOFF_KEY=c123SessionHandoff`，10 分钟 TTL）→ 写回 localStorage → 清暂存 → reload（当前页已是同一会话则只清暂存不重刷）；写回失败保留暂存不刷页（防循环），过期/坏数据即清；油猴存储不可用 → 提示到网盘页手动再导。**废弃方案留档（都是实测走不通才换的）**：① 强制跳 www.123pan.cn 且「网盘页」写死单域名——用户网盘页是 yun.123pan.cn 就全错；② 登录页 user.* 直接写入等官方跳转——**实测登录页 localStorage 不承载网盘会话，写了刷新也没用**，所谓「有 token 官方自动跳网盘」不成立。**踩坑记录**：往 bundle 插正则时 Python 转义多一层把 `\.` 写成 `\\.`（正则变成「字面反斜杠+任意字符」），域名永远不匹配且不报错——改 bundle 正则后必须跑 `tests/session-reuse.test.mjs` 验证。**废弃的第一版**：曾强制跳转 `www.123pan.cn` 且把「网盘页」写死成这一个域名——维护者实测自己的网盘页导入也误触发跳转、登录页跳过去又没完成导入，遂废弃跳转并把网盘页放宽为全部 www/yun 官方域名；弹窗文案不再写死任何具体域名。**踩坑记录**：往 bundle 插正则时 Python 转义多一层把 `\.` 写成 `\\.`（正则变成「字面反斜杠+任意字符」），域名永远不匹配且不报错——改 bundle 正则后必须跑 `tests/session-reuse.test.mjs` 验证。回归测试：`tests/session-reuse.test.mjs` 用例 5-8（导入计划/暂存-回放-清暂存/过期与非网盘页不消费/网盘页直写不暂存）。
  - **客户端会话中转接口 /api/pan/session（2026-09-14，本地待实测，属 1.3.10 + 客户端 1.4.5）**：剪贴板导出/导入对手机体验太差，改为客户端做会话中转站。调研结论先行：**123 的 OpenAPI token 与网页端会话无转换通道**（p123client 文档明确 oauth/refresh_token 类 token「只允许访问 open 接口」，网页 authorToken 是 JWT 可通吃 web/app/open；客户端手里的 OpenAPI 凭据无法解析成网页登录态），所以客户端只能「中转」网页会话、不能生成它。实现：后端 `GET/POST/DELETE /api/pan/session`（kv 键 `pan_web_session`，存 authorToken/LoginUuid/account/origin/updatedAt——origin 是推送来源网盘页、拉取端据此跳转；鉴权复用 `_guard_library_token`，与影库访问令牌同门；POST 校验会话完整性且响应里 token 打码；DELETE 清档），脚本端 `pushClientSession`/`pullClientSession`（复用 `libraryRequestUrl`/`libraryRequestJson`，走「影库接口地址 + 影库访问令牌」配置；`libraryRequestJson` 顺带支持了 POST+JSON body）——已登录浏览器「推送登录会话到客户端」存档，其他浏览器「从客户端拉取登录会话」拿到会话后走 `completeSessionImport` 同一套域名逻辑登录；手机要拉取需能访问客户端（默认只监听 127.0.0.1，跨设备需自行暴露端口并配令牌）。入口：油猴菜单「推送/拉取登录会话」+ 设置→常规 两个新按钮。回归测试：`tests/test_pan_web_session.py`（推送/拉取往返与打码/会话不完整 400/清除/与影库令牌同门）。**踩坑**：POST 路由最初只认 `body.token` 且不接受 query 令牌，而脚本统一把令牌拼在 URL query 上 → 恒 401（「影库访问令牌不正确」），修复为 `_guard_library_token(request, token or body.token)`；同日按维护者指示**删除剪贴板导出/粘贴导入**，随后**又删除设置面板的会话 UI 整节**（常规页 sessionSection、push/pull 按钮与面板方法全下线）——会话复用只留**油猴菜单**入口（推送/拉取两项），且菜单注册从 start() 末尾提前到 `configStore` 创建之后：登录页 `user.123pan.cn` 没有设置面板，菜单是唯一入口，提前注册避免后续初始化在登录页失败时把入口一起带走（`completeSessionImport`/`buildSessionExportPayload`/`parseSessionImportPayload` 保留，供拉取路径与推送使用）。
  - **秒传导入/导出并发实测（2026-09-08，浏览器登录态实测）**：`/b/api/file/upload_request`（秒传导入 reuse）**32 并发 85 req/s 全部成功、零频控** → 导入默认并发 32；`/b/api/file/list/new`（导出扫盘）有 **~15 QPS/用户频控**，超速返回 HTTP 200 + `{"code":100011,"message":""}`（**空文案**，旧重试逻辑识别不了、会直接中止导出/导入——已修：request() 把 code 100011 纳入退避重试），并发 8/16/32 成功速率都恒定 ~15 目录/秒 → 导出默认并发 8（拉高无收益）、100011 由重试兜底。
  - **分享接口风控实测结论（2026-09-08，用真实分享链接压测）**：分享页域名（`*.mshare.123pan.cn` / `*.share.123pan.cn` 等，即脚本在分享页的 `location.origin`）按窗口计数配额限制 `/b/api/share/get`：约 **60 个请求/分钟**（回补约 33/分钟），超了返回 HTTP 200 + `{"code":"429","message":"分享接口请求过于频繁"}`（无 Retry-After 头），约 60 秒自动解封；而 `www.123865.com` 实测**并发 32 持续 258 req/s 零 429**，等于没有限流，且 CORS 对任意来源放开、接口免登录。脚本修复 = 分享目录请求默认跨域直连 123865（`credentials:"omit"`，不可用再退回页面域名并会话内记住）+ 分车道限速门 `shareApiGate`（快车道零间距、目录并发默认 32；回退页面域名自动切 1s 慢车道；撞 429 全局冷却 60s 起步翻倍封顶 5 分钟、冷却后放慢到车道最大间距、大步长恢复、状态随断点持久化）+ 批量解析链接之间歇 2.5-4s。`request()` 新增 `host`/`credentialsMode`/`isRetryable` 选项，分享目录请求的 429 不允许 request 内部退避重试（会在风控窗口里继续烧配额）。
  - **旧域名分享页防跳转：尝试过、已放弃**。官方 SPA 会在旧域名（www.123865.com 等）`/s/<key>` 页面的解析阶段就发 `/gsb/s/share-key` UID 探测并 `location.replace` 到 `*.share/mshare.123pan.cn`；Tampermonkey 的注入时机晚于该内联脚本（真实浏览器链路采样证实：fetch 拦截装上时请求已发出，replaceState 挡不住已排队的导航，主动 bounce 到 `/123pan/<key>` 也会在落地页被再次跳走），用户脚本拿不到能拦跨域导航的 API。**结论：页面落在 share 域名是现状，但脚本自身接口始终直连 123865（与页面域名无关），生成秒传不受限流影响。**

## 架构备忘

- `desktop/backend/app/main.py` — FastAPI 路由层；投稿/搬运的统一分流入口是 `route_submission_text`（油猴脚本 `POST /api/submission/submit` 与后台提交共用）。
- **秒传投稿改直投二级短链，频道去 JSON 文件（2026-09-20，本地待实测，属脚本 1.3.16 + 客户端 1.4.10）**：维护者定调——秒传投稿不再走「JSON 文件发机器人」（TG 侧已不识别），频道帖不再附带秒传 JSON 文件。新流程：油猴「秒传工具箱 → 生成二级链接（短链）」生成成功后**自动直投客户端**（控制器新增 `pushFastlinkSubmissionDraft` → 复用 `submissionClient.submitShares` 同一接口同一响应校验；只推短链种子 = 单条 `123FLCPV2$%etag#体积#名.123fastlink.json` 记录，必不超 copy_text 按钮的 256 字符上限，所以草稿预览与频道帖的「秒传链接」复制按钮永远在）。**识别上下文必须随直投带上（首版踩坑）**：只推种子名会把作品识别成种子文件自己——大小显示成种子的 35.97KB、画质/视频全未识别，且种子名无 SxxEyy 会被判成电影，TMDB 电影/剧集是两套 ID 空间，同数字的电影 ID 撞出来完全另一部片（J Music 种子查出《The Jilting of Granny Weatherall》）。修法 = 核心区新增纯函数 `buildFastlinkSubmissionContext(name, text)`：从种子内容轻量正则抽真实文件名（前 100 条 `📄：`）与总体积（JSON 吃 `totalSize` 头、V2 文本按 `$`/`#` 累加，`💾：` 行），拼 `🎬/💾/📄` 上下文放进 links 条目新字段 `sourceText`（`SubmissionLinkItem.sourceText`，后端透传给 `inspect_fastlink` 的 📄/💾 解析器），与旧「JSON 发机器人」流程的识别素材对齐；`submitShares` 的 payload 映射带 `sourceText`（分享直投无此字段不受影响，老后端 pydantic 默认忽略未知字段）。**带上上下文后仍显示错片名（二轮踩坑）：识别媒体缓存按链接存、命中只比 tmdbId**——第一次识别错的电影（movie 272272）被缓存，重推时 ID 相同直接复用，画质/大小对了（来自文件名）但标题/类型还是错的那部。修法（`should_use_cached_media`）：缓存标题与本次识别标题（tmdb_title_key 精确/互含）对不上就弃用缓存重新识别；**不能用 mediaType 比对**——`infer_submission_media_type` 在文件名没有剧集信号时默认返回 movie（默认值不是证据），拿它否定缓存会误伤「无 S 记号命名 + 正确 tv 缓存」的旧场景（`test_submission_reuses_legacy_share_media_cache` 钉住）。再加一层 ID 撞库/标记打错兜底（`find_submission_media` + `_tmdb_candidates_match_title`）：ID 直查结果标题与识别标题完全对不上时补一轮标题搜索合并候选，让 `pick_best_tmdb_candidate` 评分裁决（标题命中 +40 高于类型 +20），标记打错也能救回正确条目。后端 `/api/submission/submit` 的 links 分支按 `FASTLINK_RE.match` 认秒传链接：provider=123fastlink、cleanUrl **原样保留**（绝不能走 `normalize_web_share_url`，那是给 http 分享链保留 `?pwd=` 用的），整批全为秒传时 source_label 用「油猴秒传直投」便于区分。配套去 JSON：`handle_admin_fastlink_document`（JSON 发机器人建草稿）不再给草稿挂 documents；`send_submission_seed_documents` 兜底跳过 `fastlink_json` 类型文档（旧存量草稿重发也不会再把 JSON 挂到频道）。推送失败/未配置投稿地址只 toast 警告（提示去「设置 → 分享与秒传 → 投稿地址」），不影响已生成的链接；任务最小化到后台时推送警告也照常弹 toast。回归：`test_submission_routing.py::test_direct_fastlink_links_pushed_as_fastlink_drafts`（含 sourceText 透传）、`test_fastlink_document_generates_submission_drafts`（documents 断言改为不存在）、`test_submission_drafts.py::test_fastlink_source_context_feeds_inspection`（💾/📄 上下文喂识别）、`test_media_cache_rejected_when_title_mismatches`（撞库缓存按标题弃用）、`test_find_media_searches_title_when_id_result_mismatches`（ID 结果标题不符补搜）、`test_telegram_publish_deletes_previous_channel_post_from_database_history_even_when_fastlink_changes`（频道发布不再发 JSON 文档，seedMessageIds 空）、油猴 `fastlink-secondary.test.mjs` 直投上下文三用例。历史清理不受影响：本地发布记录（messageId+seedMessageIds）照常落库，TG API 旧帖扫描按 caption 里的 TMDB 标记匹配（fastlink 帖按钮形状 copy_text 与旧帖一致，`publication_button_matches` 兼容）。
- `desktop/backend/app/transfer_service.py` — 搬运任务调度门面：入队、去重、并发、暂停窗口、115 账号池健康、直链轮换、通知钩子。
- `desktop/backend/app/transfer_pipeline.py` — 单次搬运的六阶段管线：解析 → 规划 → 秒传 → 离线 → 等待 → 收尾；`OfflineDownloadManager` 滚动提交 + 统一等待（同时在 123 排队的离线任务数上限 = 后台"并发"配置 1-5，完成一个自动补交；完成判定 = 列目录 + 大小比对）。
- `desktop/backend/app/logsetup.py` — 统一日志：控制台 + 落盘轮转 + 内存环形缓冲（`GET /api/logs`）。应用日志必须是**大白话流程叙述**，第三方库一律降噪到 WARNING。
- `desktop/backend/app/telegram_session.py` — TG 用户 Session 获取：手机号 → 验证码 →（可选两步验证密码）→ StringSession，登录会话只在内存里活 10 分钟。配套接口在 main.py：`/api/submission/telegram/session/start|verify|cancel`；登录成功后 session 由后端直接写回投稿配置的 `telegramApi`，并调 `reset_telegram_client_state()` 让旧帖清理的 client 单例重建。
- 离线等待阶段**不打印心跳日志**：后台「离线轮询间隔」默认 15 秒，每轮一条「…秒后再检查」会把每个文件的成功/失败全部淹掉。现在只保留成功/失败；每 60 秒做一次静默存档，仅用于及时感知任务被删除/取消。
- Telegram 有两条通道：**投稿入口走 HTTP**（油猴脚本 `POST /api/submission/submit`，与后台共用 `route_submission_text` 分流），**最小化轮询**（`telegram_callback_polling_loop`）只处理草稿预览按钮回调和按钮触发的后续输入，不接收投稿文本；搬运任务通知、投稿预览、频道发布均为出站。
- `desktop/backend/app/movie_library_db.py` — 影库数据库层（**数据库优先架构**）：三张表 `library_sources`（来源）/ `library_works`（作品，dir 主键）/ `library_work_files`（作品内文件），都在同一个 `cloud123.db`（WAL）。导入规则：dir 已存在则跳过（保留先入库的），同名来源重新导入 = 先清该来源旧数据再插（可更新），删除来源级联删其作品与文件。搜索走 `norm_title/pinyin` 索引 + SQL 分页，分类 GROUP BY、统计 SUM，导入时就算好拼音与归一化标题。
  - **影库导入优化二批（2026-09-14，本地待实测，客户端 1.4.3 本地构建）**：① 只认影库文件，其他默认跳过——`/api/library/import/paths` 与 `/import/dir` 里解析失败（ValueError，如「不是影库索引文件」「缺少 files 字段」）不再计入失败，记为「跳过/非影库文件，已跳过（原因）」；只有 IO 错误才算失败。② 视频扩展名默认放宽：`VIDEO_EXT` 新增 m4v/m1v/m2v/m2p/mp2/mpe/mpv/mxf/ogm/ogv/tp/trp/qt/dv/amv/svi/3g2。③ 视频扩展名收敛为 PT/BT 站主流集合（mkv/mp4/ts/iso 原盘/m2ts/avi/rmvb/wmv/mpg/mpeg/m4v/mov/flv/webm/vob/f4v/divx/rm/mts/m2t，去掉 mxf/tp/trp/qt/dv 等冷门容器），影库设置新增「自定义视频扩展名」：配置键 `movieLibrary.videoExtensions`（逗号/分号/空格/顿号分隔，带点不带点均可，`normalize_video_extensions` 清洗，**注意 frozenset 不是 set 的实例**——isinstance 分支要显式含 frozenset，踩过一次），`video_ext_set()` 与内置默认合并，贯通 `aggregate_works`/`import_payload`/`import_stream`（video_count 与 is_video 随配置生效，重新导入才落库）；GET/PUT `/api/library/config` 往返透传，前端 LibraryView 影库设置卡新增输入框（提示语明确：判定对象是导入 JSON 里的文件条目，不是筛哪些 JSON 能导）。④ 头部 `@description` 精简为一句话导流 README（细节全在 README）；`油猴脚本/123-helper/README.md` 与 `desktop/README.md` 重写：保留全部功能说明，补齐使用方式（含会话复用流程、窗口保活说明、线路开关），语气收敛去 AI 腔。回归测试：`tests/test_movie_library.py::MovieLibraryStreamImportTests::test_video_extensions_config_and_import`。
  - **客户端关窗再开黑屏修复（2026-09-14，本地待实测）**：关窗保活（红绿灯关闭只 destroy 窗口、后端继续跑）后点 Dock 由 `activate` 重建窗口，但 `/admin` 只在**启动时**后端首次健康通过后加载一次——重建的窗口停留在 about:blank，只剩 `backgroundColor #0a0c12` 深色底 = 用户看到的黑屏。修复：`createWindow()` 里检测 `backend.port` 已存在（保活重建场景）时主动 `waitForHealth(30s).then(loadAdmin)`（超时也照常加载，让页面自己呈现错误），冷启动（port 未定）仍走 whenReady 原路径。回归方式：打包版关窗 → 点 Dock 重开，应直接进管理页。
  - **客户端自动更新闭环（2026-09-14，本地待实测，客户端 1.4.5 本地构建）**：1.4.4 已接线 electron-updater（GitHub provider，启动 10s 后 + 每 6h 检查，自动下载，设置页「软件更新」卡展示进度与「重启并安装」），但 mac 链路走不通，两个根因：① `MacUpdater.doDownloadUpdate` 只认 zip（`findFile(files, "zip", ["pkg", "dmg"])` 无 zip 直接抛 `ERR_UPDATER_ZIP_FILE_NOT_FOUND`），而打包只出 dmg → 检测到新版必失败，被 `classifyUpdaterError` 归为 update-manual（就是用户看到的「该版本未包含自动更新组件，请到发布页下载安装」）；② 就算补了 zip，MacUpdater 安装也走 Squirrel.Mac 换包并校验签名，本应用只有 afterSign 钩子做的 ad-hoc 签名（identity: null + `codesign --sign -`），过不了校验。修复：① electron-builder mac target 改 dmg+zip 双产物（dmg 供首次/手动安装，zip 供自动更新；CI 产物上传与 Release 发布同步补 `*.zip`）；② mac 安装绕开 Squirrel——`installMacUpdateBySwap()` 把 updater 已 sha512 校验的 zip 交给 detached bash 脚本：等本进程退出（轮询 kill -0）→ `ditto -x -k` 解压到安装目录同卷 stage → 先 `mv` 旧 .app 做备份再换入新包（失败自动回滚）→ `open` 重启；换包前手动 `backend.stop()`（`app.exit()` 不触发 before-quit，与 app:relaunchApp 同款套路）；`autoInstallOnAppQuit` 在 darwin 置 false，防 MacUpdater 在下载完成时就把更新递给 Squirrel；Windows 修复同类坑：`BaseUpdater.quitAndInstall` 是**先 spawn NSIS 静默安装器、之后才 app.quit()**，sidecar `cloudgateway.exe` 不先停会锁住安装目录文件导致静默安装失败，所以「重启并安装」前显式 `await backend.stop()`（退出时自动安装走 app 'quit' 事件，此前 before-quit 已等后端停完，天然安全）。换包日志落 `dataDir/update-install.log`。数据不丢：userData 固定在 `appData/123Cloud`，与 app bundle 位置无关。E2E 验证法：改 app 内 `Resources/app-update.yml` 为 `provider: generic, url: http://127.0.0.1:PORT`，本地起静态 feed（latest-mac.yml 版本号写高一个补丁位、sha512 对上真实 zip）即可整链实测检测→下载→换包→重启。
  - **影库导入提速（2026-09-14，本地待实测，客户端 1.4.3 本地构建）**：JSON 导入全链路流式化——`movie_library.open_library_stream(path)` 按块读 + `JSONDecoder.raw_decode` 逐条解出 files 元素（标准 123FastLink / MegaShare / `[etag,size,path]` 数组三种形态；strm-docker `libraries[]` 嵌套抛 `LibraryFullParseFallback` 由 `_import_library_from_path` 回退整读），条目归一化/作品聚合根切分抽成 `_normalize_entry`/`split_work` 与整读路径共用同一实现；DB 层新增 `import_stream`：条目按批（2 万）`executemany` 写 `library_work_files`，作品行按累计统计最后统一 executemany 写入，dir 去重逐作品缓存判定（每根只查一次库），语义与 `import_payload` 严格一致（回归测试做了双库逐行比对）。`/api/library/import/paths` 与 `/import/dir` 走流式，`/api/library/import`（浏览器上传）保持整读但入库挪进 `asyncio.to_thread`——三个路由都不再阻塞事件循环。**实测（200 万条 / 247MB）**：峰值内存 225MB（旧整读 json.loads 一步就 2GB，2.45GB/1400 万条的旧路径要 20GB+ 必崩）、入库 ~5 万条/秒。注意 macOS `ru_maxrss` 单位是字节。回归测试：`tests/test_movie_library.py::MovieLibraryStreamImportTests`（流式≡整读逐条比对/数组形态/跨批次聚合/双库逐行一致/去重跳过/重导更新/空 files/截断/libraries 回退/路由 helper）。
  - **影库频道六分 + 状态维度下线（2026-09-18，本地待实测，属 1.4.7）**：维护者反馈「更新中/已完结不准、电影剧集两种太粗」。**频道**：`library_works.media_type` 的语义从 movie/tv 升级为中文频道——`movie_library.normalize_channel(matched_type, tmdb_info)`（enrich 时用，genre id + zh-CN/en 名都认：16 动画→动漫、99 纪录→纪录片（电影剧集都适用，动画电影/纪录电影也进对应频道），剧集再分 10762 儿童→儿童、10763/10764/10767 新闻真人秀脱口秀→综艺，其余 tv→电视剧、movie→电影）+ `channel_from_stored(matched_type, genre_names)`（旧数据回填用，只匹配 genres 列里已存的 zh-CN 中文名）；**旧库打开时 `_migrate` 自动把 media_type ∈ {movie,tv} 的行按已存 genres 回填成中文频道**（跑完不再命中 movie/tv，天然幂等，不用重拉 TMDB、不用重导入）；`_tmdb_enrich_fields` 之后写入的就是频道。**状态维度**：`normalize_air_status`/air_status 字段保留（详情仍可显示），但 facets 不再返回 statuses 计数（恒空数组占位）、前端筛选面板删掉「状态」行、search 的 status 参数保留兼容但 UI 不再传。前端 CHANNEL_LABEL 只兜底 movie→电影/tv→电视剧，中文频道值直接显示；`TV_CATS`（海报 emoji 回退）补纪录片/综艺/儿童。注意：搜索接口的 mediaType 参数从此按中文频道过滤（旧 movie/tv 值迁移后搜不到）。回归：`test_movie_library.py` 的 `test_tmdb_enrich_channel_buckets`/`test_channel_from_stored_migration`/`test_media_type_channel_migration_on_open`/facets 与频道搜索断言。
  - **影库数据管理：海报墙删除 + 数据库备份恢复（2026-09-18，本地待实测，属 1.4.7）**：维护者定调——JSON 导进来就进数据库，**按「来源 JSON」删除的入口取消**（设置页来源列表只读；`POST /api/library/sources/delete` 后端路由保留但前端不再调用），删数据库内容只有两条路、都在「搜索浏览」tab 的**管理模式**（搜索行「管理」按钮进入，卡片变多选）：①多选删作品（`POST /api/library/works/delete`，一次 ≤200，confirm 后调 `movie_library_db.delete_works`——works/files/playback 三表级联，只动本地库不动网盘与源 JSON）；②整分类删（`POST /api/library/category/delete` {cat, sub}，管理栏下拉选分类（来自 categories 接口，「全部文件」合成分类不许删），`delete_category` 按 cat[+sub] 捞 dir 再走 delete_works）。**数据库备份恢复**（重装不丢 TMDB 整理结果与播放记录）：`movie_library_db.export_backup(dest)` 把 sources/works/files/playback **四张表原样**导出成独立 sqlite（`影库备份-YYYYMMDD-HHMMSS.db`，写在导出目录；**必须先删旧文件再建**——建表是 IF NOT EXISTS，覆盖导出会混入旧数据）；`import_backup(src)` ATTACH 备份后 `INSERT OR IGNORE` 恢复 sources/works/files（dir 已存在跳过、保留先入库的），**播放记录走 upsert 且保留 updated_at 较新的一条**（`INSERT ... SELECT ... WHERE true ON CONFLICT DO UPDATE ... WHERE excluded.updated_at > library_playback.updated_at`——SELECT 形式的 upsert 必须带占位 `WHERE true` 消除 ON CONFLICT 解析歧义）；备份文件先 sqlite_master 探测 library_works/files 表存在，非 sqlite 或缺表报「不是影库备份文件」。路由 `POST /api/library/backup/export|import`（import 走桌面 pickFiles 选 .db 文件传 path）。回归测试 `tests/test_library_manage.py`（级联删除/分类删除含守卫/导出导入全新库逐行一致/跳过已存在+播放记录新旧取舍/坏文件拒绝/覆盖导出不残留/路由与令牌门卫）。
  - **影库播放闭环（2026-09-18，属 1.4.7 已发布，留档）**：海报墙点播 → 后端把**当季**文件秒传进网盘播放缓存目录（默认**沿用网盘原有的「秒传」目录**（维护者 2026-09-18 指定不新建文件夹）：`秒传/剧名 (年)/Season NN/`，电影为 `秒传/剧名 (年)/`；配置键 `movieLibrary.playCachePath` 可改，早期本地构建存过「影库播放」的在 normalize 里自动迁移成默认值）→ 生成本地 m3u 交给本地播放器连续播放；每集开播时后端按需取 123 直链（`download_info`，55 秒缓存）**302 重定向**——与 123pan-strm-docker 的 /play 思路同源：播放列表永远指向本机服务（`/api/library/play/{会话}/playlist.m3u` 与 `/{序号}` 两个端点**不走影库令牌门卫**，128 位随机会话 id 即凭证，播放器发不了自定义 header；后端默认只监听 127.0.0.1）。核心模块 `app/library_playback.py`：`detect_player`（配置 `movieLibrary.playerPath` 优先，按文件名判类型；mac 自动检测 IINA(iina-cli)→mpv→VLC，win/linux 走 PATH）+ `PlaybackService`（转存前先 `list_files` 按**文件名+体积**比对，已有复用拿 fileId，缺失 `md5_reuse`（间隔 transferIntervalMs），秒传未命中剔除并回报；**同名但体积不一致的跳过不转存**（防 123 生成「名字 (1)」副本））+ `MpvIpcMonitor`（mpv/IINA 用 `--input-ipc-server`（IINA 走 `--mpv-` 前缀透传）回传播放进度：每 2 秒 `get_property` playlist-playing-pos/time-pos/duration/percent-pos → 落库集内秒数；**≥92% 或切到下一集即判看完** → `trash_files` 把该集网盘文件移入回收站（配置 `movieLibrary.autoTrash` 默认开，回收的是自己转存进缓存目录的文件，按记录里的 cloud_file_id）；VLC 无回传 → 退化手动「标记已看」。监听线程独立跑（unix socket / win 命名管道），回调用 `run_coroutine_threadsafe` 回主循环做异步回收站操作）。季/集解析纯函数在 `movie_library.py`：`parse_season_episode`（S01E02/1x05/EP03/第N集/中文数字集、季目录 Season N/SNN/第N季/中文数字季；**有集无季默认第 1 季**；`(?<![0-9a-z])` 防误命中 Se7en/1920x1080）+ `order_play_items`（剧集按季分组、同集多版本默认取体积最大、解析不出的花絮不进播放列表；电影无季集标记 → standalone=体积最大的单文件）。播放记录新表 `library_playback`（主键 dir+file_path，season/episode/cloud_file_id/position_sec/duration_sec/watched）。起播选择：显式指定季/集/文件 → 精确起播；海报墙「播放」（resume=true）→ 跳过已看前缀从第一个没看完的集续播（断点 ≥30 秒且 <92% 时 `--start=秒数` 续播）。前端 LibraryView：海报卡 hover 播放按钮 + 「续看/看到/已看完」角标、页顶「继续观看」横滑栏（`/api/library/progress/continue`）、详情弹窗季海报墙（TMDB 季详情代理 `/api/library/tmdb/{id}/season/{n}` + kv 缓存 `movieTmdbSeason:{id}:{n}`）→ 集列表（TMDB 分集名、已看勾、断点进度条、从此集播放）、电影单条目播放与已看开关、文件列表每行「播放此文件」（指定版本用）；播放中每 5 秒静默刷新集列表。**会话仅内存**（后端重启后旧播放列表失效，重新点播即好），播放记录持久在 cloud123.db。已看集被移回收站后重看：下次点播的转存比对发现缺失自动重新秒传，自愈。回归测试：`tests/test_movie_library_playback.py`（季集解析/排序与版本择优/播放记录表/转存去重与未命中剔除/m3u 与起点切片/续播跳已看/手动标已看触发回收站/IPC 监听纯逻辑（≥92%、切集判上一集已看、不重复）/播放器命令组装/路由（令牌门卫、未授权 400、m3u 与 302、structure/progress/summary/continue、TMDB 季代理缓存、配置新键往返））。**首轮回访修复（2026-09-18，本地待实测）**：① 播放器校验前置——没装播放器时先报错再转存（旧逻辑转存完一整季才报「没找到播放器」，表现为「卡一会没动静」），且新增「预检失败不写任何播放记录」；② **播放记录只落真正交给播放器的第一集**（旧逻辑给整季每集都 upsert 一条记录，「续看」角标直接跳到最新一集——维护者实测命中；没播到的集保持无记录，看完/进度仍由 IPC 回传或手动标记逐集落库）；③ **任意播放器**：`detect_player` 支持 `.app` 应用包（直接选 /Applications 里的 Infuse 等，kind=app，`open -a` 启动）与 `system`（系统默认播放器，mac 走 `/usr/bin/open`、win 走 `cmd /c start`），这类播放器没有 IPC 回传（无进度、无自动已看，续播只到集），Electron 新增 `app:pickPlayer` 桥（properties 含 openDirectory 才能选 .app）；测试里类级 mock 会盖住模块函数，顶层先存原函数引用再测分支。**二轮回访补充（2026-09-18，本地待实测）**：④ **Windows 自定义播放器**：`detect_player` 的自定义路径分支本身跨平台（exe 按文件名判 mpv/vlc，不认识的如 PotPlayer 记 kind=other，显示名用程序文件名，启动命令 = `[exe, 播放列表地址]`），「系统默认」win 走 `cmd /c start`；前端「选择播放器…」用 `app:pickPlayer` 桥（openFile+openDirectory），Windows 选 exe、mac 选 .app 都能选；⑤ **「看到第 N 集」一键标记**（维护者 2026-09-18 要求：集可以手动选择看到哪一集）：季集列表头部「看到哪一集…」→ 选集号确认 → `POST /api/library/progress/mark-until` {dir, season, episode} → `PlaybackService.mark_watched_until`：以 order_play_items 的全集列表为准（无记录的集补建记录），该季 1..N 标已看（position 清 0）、N 之后已看的取消已看，其他季不动；新标已看且转存过的文件按 autoTrash 照常 `trash_files`（`watched_cloud_ids` 先查 cloud_file_id，回收后清零），返回 markedWatched/markedUnwatched/trashed 供提示。回归：`test_movie_library_playback.py` 的 `test_mark_watched_until`、`test_detect_player_windows_custom_exe`。**三轮回访补充（2026-09-18，本地待实测）**：⑥ 详情弹窗删掉 airStatus chip（「更新中/已完结」不准，随状态维度一起下线）；⑦ **细粒度技术属性**（维护者要求把油猴脚本整理识别的属性拿过来）：`movie_library.infer_technical_detailed(file_names)` 从文件名现算作品级 `resourceType`（UHD BluRay Remux > BluRay Remux > Remux > UHD BluRay > BluRay > WEB-DL > …）、`dolbyVision`（DoVi/Dolby Vision/DV→DV）、`dynamicRange`（HDR10+ > HDR10 > HDR.Vivid > HDR > HLG > SDR > EDR）、`videoCodec`（AV1 > HEVC > H265(X265) > AVC > H264(X264) > MPEG-2 > VC-1）、`audioCodec`（TrueHD > DTS.X > DTS.HD.MA > DTS.HD.HRA > DDP(EAC3/DD+) > DD > DTS > FLAC > AAC > LPCM > Opus > AV3A）、`frameRate`（**含油猴同款帧率修复**：`H.265.25fps` 的左最匹配把版本号并进数字 → 只留 fps 紧前一段=25fps，真实小数帧率 23.976fps 不受影响）、`highQuality`（HQ）、`originalEdition`（IMAX/CC/Director's Cut/Extended/Theatrical/Unrated/Open Matte/Hybrid/Remastered/PROPER/REPACK/RERIP，全出现的都收）；作品级取全部文件命中的最高档（originalEdition 取并集按固定顺序）。`/api/library/play/structure` 响应新增 `tech` 字段，前端详情 chips 渲染（资源类型 chip 优先用 tech，退化用入库的 edition；不分库、纯展示、点开详情现算）。⑧ **已看入口可见性**：集行左侧圆圈图标旁加「已看/未看」文字（`.episode-check.on` 绿色），电影模式同款。⑨ **技术属性可筛选**（维护者 2026-09-18：「这些信息在分类也可以筛选」）：`library_works` 新增 `tech` 列（紧凑 JSON：resourceType/dolbyVision/dynamicRange/videoCodec/audioCodec/frameRate/highQuality/originalEdition），`infer_technical_detailed` 重构为增量状态机（`new_detail_state`/`update_detail_state`/`detail_state_result`，流式导入逐文件喂、不驻留全集文件名），import_payload/import_stream 入库时写入；旧库 `_migrate` 补列后对 `tech=''` 的作品按 library_work_files 文件名回填（一次遍历，之后幂等）。筛选：search/facets 新增 `tech` 参数（格式 `字段:值`，SQL `tech LIKE '%"字段":"值"'` 键值精确匹配防 HDR 命中 HDR10 前缀误伤；originalEdition 特例 LIKE 单值带引号）；facets 返回 `effects`（DV+dynamicRange 合并一行）、`videoCodecs`、`audioCodecs` 三行计数，交叉筛选时隐藏与当前 tech 筛选同字段的值。前端面板加「特效/编码/音轨」三行（activeTech 存 `字段:值`，随 UI state 持久化）。真实侧车验证：导入 DoVi 剧名 → effects {DV:1, HDR10+:1}（作品级 dynamicRange 取最高档 HDR10+）、search tech=dolbyVision:DV 命中。回归：`test_movie_library.py` 的 `test_tech_filter_and_facets`。**四轮回访修复（2026-09-18，本地待实测）**：⑩ **播放器设置重进就丢**（维护者实测）：两个根因叠加——前端「选择播放器…」只写输入框没保存（保存按钮在另一个输入框旁容易漏点）；后端 `LibraryConfigRequest` 全字段默认值语义下**任何部分字段的 putConfig 调用（如固定导出目录）都会把 playerPath 清空、autoTrash 置 false、缓存目录回默认**。修法：`LibraryConfigRequest` 全字段 `Optional`，**None=未传保留现值、显式空串=清空**（playerPath 空→自动检测、playCachePath 空→默认「秒传」；transferIntervalMs=0 等 0 值合法注意 is not None 判断），PUT 逐字段合并 current；前端「选择播放器…/系统默认/自动检测」选完**立即保存**（`savePlayerPath` 部分字段提交），autoTrash 开关切换即存（`saveAutoTrash`），`saveConfig` 全字段保留。回归：`test_config_new_playback_keys`（无参全保留/空串清空）+ `test_config_partial_update_keeps_player`。⑪ **IINA 报「找不到流」**（维护者实测）：手动选择在 Finder 里选的是 `IINA.app` 应用包 → kind=app → `open -a /Applications/IINA.app http://…m3u`，**IINA 不接收 open -a 方式给的网络流地址**；修法 = `detect_player` 对 `.app` 包特判 IINA：找到 `Contents/MacOS/iina-cli` 就用 kind=iina 直接 exec（既修播放又恢复 IPC 进度回传）；其他 .app（Infuse 等，声明了流媒体处理）保持 open -a。回归：`test_detect_player_iina_app_uses_cli`。**五轮回访修复（2026-09-18，本地待实测）**：⑫ **IINA/Infuse 都报「无法打开文件或流媒体」（维护者实测截图）**：本机装 mpv 做真实实验——用线上同款 playlist_m3u/resolve_item 构造最小服务，mpv 完整走通「拉 m3u(200) → 识别 → 播第一项 → 302 → 跟随拉流(200)」整链，**服务端链路结构无问题**，真凶锁定 OpenAPI `download_info` 取直链失败（错误此前被吞）。修法：① **网页版直链备胎**——参考项目 123pan-strm-docker 取直链走的是**网页接口** `POST yun.123pan.com/b/api/file/download_info`（body 带 driveId/etag/fileId/s3keyFlag/type/fileName/size，吃 authorToken+platform:web 头），不是 OpenAPI；`library_playback.web_download_url` 照抄该接口，`PlaybackService` 加 `web_session_provider`（main.py 传 `store.read_value(PAN_WEB_SESSION_KEY)`，即油猴「推送登录会话到客户端」的存档），`resolve_item` 降级链 = OpenAPI → 网页接口 → 报错带两边原因；播放条目补存 etag/s3KeyFlag（`movie_library_db.list_files` 补返 s3KeyFlag 列）。**备胎生效前提：油猴脚本里做过「推送登录会话到客户端」**（网页 authorToken 存档）；② m3u/302 端点与直链解析全加日志（失败带 OpenAPI/网页两边错误与 fileId），复现时看「运行日志」即可定位。回归：`test_resolve_item_web_fallback`、`test_resolve_item_all_sources_fail`。**六轮回访（2026-09-18，本地待实测）**：⑬ **换播放器后服务端仍无任何拉流日志**（维护者日志截图：两次播放只有「开始交给 Infuse/IINA」，无直链日志）→ 播放器请求根本没到服务端。真凶：**播放列表地址用了 request.base_url**——管理页若从 `http://localhost:端口` 打开，m3u 里的条目就是 `http://localhost:…`，播放器解析 localhost 优先 IPv6(::1)，而后端只监听 127.0.0.1，连接被拒（IINA「无法打开文件或流媒体」、Infuse「加载此内容时发生错误」，两播放器同挂且服务端零日志）。修法：`start_library_play` 路由强制 `base_url = http://127.0.0.1:{端口}`（端口取请求 Host 头，回退 CLOUD123_PORT），m3u 拉取成功也记 INFO 日志（「播放器已拉取播放列表」，下次复现一眼看出播放器来没来）。⑭ **仍无拉流日志的排查加固（2026-09-18）**：本机装 mpv + IINA 复核——mpv 带 `--input-ipc-server=/tmp/x.sock`（等号形式）+ URL 完整走通（IPC 监听/拉 m3u/302/拉流），`iina-cli` 源码（iina-cli/main.swift）确认 `--mpv-` 前缀参数与 URL 均原样转发给 IINA 主程序（URL 有 Regex.url 检测保留、`-` 开头参数不转路径），**播放器侧与参数格式无问题**；「开始交给播放器」日志行补打播放列表地址（跑旧包时地址会带 localhost，一眼识别版本）。⑮ **IINA 不发请求的真凶（2026-09-18 实锤）**：本机装真 IINA 复现——IINA 内核 IPC `get_property playlist` 显示 **m3u 已加载、条目 `playing:true`，但服务端零请求**（卡在打开条目之前的 hook 阶段）；进一步实测 **IINA 内置的 `youtube-dl` 二进制挂死**（`--version` 都跑不完）——IINA 对每个 http 地址先跑 ytdl_hook 解析，解析挂死 → loadfile 永远不进网络层 → 超时报「无法打开文件或流媒体」。修法（IINA 社区标准处方）：mpv 传 `--ytdl=no`、IINA 传 `--mpv-ytdl=no --no-stdin`（iina-cli 的 stdin 探测不可靠，官方帮助明说「sometimes not」，误判时插 --stdin 会让 IINA 等标准输入不播文件）；另把 m3u 写成本地文件（`DATA_DIR/playlists/{会话}.m3u`，`_write_playlist_file`，会话过期清理）交给播放器——本地文件走 IINA/Infuse 最成熟的「文档打开」路径，条目仍是本机服务地址由内核逐项拉取（mpv 同路径实测通）；Infuse 的「尚不支持此类型文件」同属打开方式问题，本地 m3u 是它的标准输入。测试更新：build_player_command 断言 `--ytdl=no`/`--no-stdin`/`--mpv-ytdl=no`、start_play 断言播放器拿到本地 m3u 文件。⑯ **Infuse 专项 + Windows 加固（2026-09-18，本地待实测）**：本机装真 Infuse 实测——**Infuse 拒绝 .m3u 文件与命令行 URL（零请求，「尚不支持此类型文件」）**，它只认自家跳转协议 `infuse://x-callback-url/play?url=<编码后的流地址>` 且只保证单条流。修法：`detect_player` 对 Infuse.app 特判 kind=infuse；`start_play` 单集模式——只转存/播起播那一集（items_src 截 1），启动命令 `open "infuse://x-callback-url/play?url=…"`（302 链路复用），返回 `note`「Infuse 暂不支持连播列表」由前端 1.2 秒后 toast 提醒（其余播放器连播不变）。Windows 加固：m3u 文件写 **UTF-8 BOM**（utf-8-sig，PotPlayer/VLC 认 BOM 才稳收中文 EXTINF）；Windows 播放器（mpv/VLC/PotPlayer）吃本地 m3u+http 条目是最基础能力、mpv 同款内核已完整实测，**无 IINA 式 youtube-dl 私有组件问题，风险很低**；「系统默认」win 走 `cmd /c start` 文件关联。回归：`test_detect_player_custom_paths`（infuse kind）、`test_start_play_infuse_single_episode`。⑰ **「续看」角标语义修正（2026-09-18，本地待实测）**：维护者日志确认回传链路是通的（「S01E01 看完，已移入回收站」即 IPC 回传触发），坏的是角标——E01 已看完角标却显示「看到 S01E01」（旧逻辑取 updated_at 最新一条记录）。修法：`playback_summaries` 扩展 `nextSeason/nextEpisode`——拉作品全部视频文件名解析季集（parse_season_episode），与已看集合合并后按序取第一个没有「已看记录」的集（与 start_play resume 的实际起播一致，没记录的集也算候选）；前端 playBadge/continueLabel 优先用 next（「续看 S01E02」），全看完才显示「已看完」。回归：`PlaybackDbTests` 的 next 断言。⑱ **「续看 S01E02」错到第 4 集（维护者实测，本机真实库定位）**：`library_playback` 里 E02 行的 season/episode 列被历史版本某次「标记已看」写成了 0/0（watched=1 还在）——已看集合里它成了 (0,0)，(1,2) 不在已看集合 → next 错算成 E02。修法：① `_migrate` 把 season=0 AND episode=0 的行按 file_path 重新解析回填（幂等）；② `mark_watched` 的 upsert 带上季集号（记录存在用记录值、不存在按路径解析），不再产生 0/0；③ `playback_summaries` 的已看键容错（列 0/0 时按路径解析）。真实库验证：修复后 next=续看 S01E04 ✓、E02 行修正回 (1,2) 且 watched 保留。
- `desktop/backend/app/movie_library.py` — 影库解析/格式工具（移植自"123云盘影库搜索-本地版"）：只保留纯函数（`parse_library_content` 等 123FastLink / 123pan-strm / V1/V2 秒传文本 / `.123share` 解析，`{tmdb-N}` 与 `[tmdb-N]` 都认）、`aggregate_works` 按 tmdb 标记聚合（Season 并入）、`split_category`、`fmt_size`、拼音键。**已删除** `Library`/`LibraryManager`/`MovieLibraryService`/watcher：不再有"扫描指定文件夹 + mtime 热重载 + kv 索引缓存"那套，用户手动选 JSON 入库后源 JSON 删掉也能查。
- 配套 `library_transfer.py`（复用已授权 123 OpenAPI 的 `md5_reuse`+`ensure_path` 转存到云盘）。**客户端的「分享链接提取入影库」功能已移除**（`share_extractor.py` 模块与 `/api/library/share/*`、`/api/library/file-types` 路由已删，`library_share_history` 表不再建；影库只支持本地导入：浏览器上传 / 文件选择器 / 文件夹批量），油猴脚本侧的「分享链接免转存生成秒传」不受影响、保留在脚本里。
- 路由在 main.py `/api/library/*`：数据接口（search/files/export/categories/transfer）在配置了 `movieLibrary.token` 后必须带令牌（query `token=` 或 Bearer，恒时比较）；`GET /api/library/config` 的明文令牌只回给 127.0.0.1 来源。导入入口三个：`POST /api/library/import`（浏览器上传原始内容）、`POST /api/library/import/paths`（桌面端文件选择器多选，后端按路径读）、`POST /api/library/import/dir`（文件夹一次性批量导入，递归子目录、跳过 `_checkpoints`/隐藏目录、深度 ≤3，不驻留监控）；来源管理 `GET /api/library/sources` + `POST /api/library/sources/delete`（也支持 DELETE 方法）。`POST /api/library/scan` 与配置里的 `folder`/`autoScan`/`intervalSec` 已移除（导出目录 `exportDir` 保留，留空则落到 `DATA_DIR/秒传文件导出`）。影库接口面向油猴脚本"随时随地搜索转存"，与投稿接口同源同端口（`设置 → 服务端口`），客户端保持默认 127.0.0.1 监听。海报与 TMDB 详情走后端代理（`/api/library/poster|tmdb/{id}`，客户端投稿配置的 TMDB TOKEN，sqlite kv 缓存）。
- 油猴脚本（`油猴脚本/123-helper/123-helper.user.js`）不改动：它对 `/api/submission/submit` 的响应有硬校验（`ok===true` 且 `draftCount`、`sentCount` 必须等于批次条数），后端必须保持这个响应契约。
